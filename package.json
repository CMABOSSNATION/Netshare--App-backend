{
  "name": "netshare-relay-worker",
  "version": "3.1.0",
  "description": "NetShare relay — Cloudflare Workers + Durable Objects (Free Plan)",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
