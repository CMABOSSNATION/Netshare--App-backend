{
  "name": "netshare-relay-worker",
  "version": "3.0.0",
  "description": "NetShare relay rewritten for Cloudflare Workers + Durable Objects",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
